# 安全说明

当前 Starter 已实现搜索、导航及 DOM 工具，仍未经过独立生产安全审计。

## 隔离边界

首版将 HtmlUnit 嵌入主 JVM，按顶层 ChatClient 调用隔离 WebClient、Cookie、页面、存储及元素引用。此机制用于防止正常调用中的跨用户状态混用，不构成抵御恶意页面的 OS 沙箱。

线程共享进程权限和资源。引擎漏洞、内存耗尽、无法中断的脚本都可能影响宿主应用。需要强安全边界的部署应在独立受限进程、容器或其他沙箱中执行页面；当前内嵌路线不提供这一保证。

[HtmlUnit 官方安全说明](https://htmlunit.org/security.html)也明确指出其不应作为不可信网页的安全沙箱。

## 已实现的约束与剩余边界

- 会话对象由调用生命周期内部创建；模型不能指定其他用户的会话，页面和引用只在当前会话查找。
- 不向页面传递宿主应用凭据或任意 Java 对象访问能力。
- 在 HtmlUnit WebConnection 层对 HTTP(S) 请求检查地址，默认拒绝内网/保留地址、URL 凭据及其他协议，WebSocket 关闭。但 DNS 检查未固定最终 socket IP，不能保证抵御 DNS 重绑定；部署仍需出站网络限制。
- 正文、输入、并发会话、页面、排队量和操作次数有上限。4 MiB 响应检查发生在下载后，无法防止全部下载/解压资源消耗。Future.cancel 不能强制终止所有引擎执行，关闭完成前不归还会话配额。
- 正式代码不主动记录查询、页面正文、Cookie 或模型凭据；应用和第三方库的日志需由部署方配置。联网测试只使用公共文档并输出结果。
- 网页正文、搜索结果和 DOM 属性都是不可信输入；工具描述明确提示，但这不构成提示注入防护。点击及表单提交的业务授权由调用应用决定。
- 浏览器配置覆盖只涉及 BrowserVersion 字段，不能改变网络策略和其他会话的访问权；不提供任意 JavaScript 执行或文件上传工具。

## 漏洞报告

请勿在公开 Issue 中发布敏感数据或可直接利用的漏洞细节。若仓库启用 GitHub Private Vulnerability Reporting，请通过 Security 页面私密提交；否则请先在 Issue 中仅请求维护者提供私密联系渠道。
